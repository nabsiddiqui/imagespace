# Minimum supported dependency versions.
#
# The CI "minimum-deps" job installs the package under these constraints to
# prove the pipeline still runs on the oldest versions we claim to support.
# Keep these in sync with the lower bounds in pyproject.toml.
pillow==9.5.0
numpy==1.24.4
scikit-learn==1.3.2
opentsne==1.0.0
hdbscan==0.8.40
scipy==1.10.1
pytest==7.0.1
