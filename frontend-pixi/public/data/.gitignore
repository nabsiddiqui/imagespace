# Atlas textures — generated by pipeline, too large for git (use Git LFS or deploy dist/ directly)
*.webp
# Binary layout file — generated by pipeline
data.bin
# Old v1 format (replaced by data.bin)
data.json
# Manifest, metadata, cluster labels are small enough to commit if desired
manifest.json
metadata.csv
